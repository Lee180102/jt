<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <settings>
        <!-- 开启驼峰自动映射
            字段:user_id   pojo userId  自动驼峰映射
         -->
        <setting name="mapUnderscoreToCamelCase" value="true" />
    </settings>
</configuration>
